A library that implements Jacquin's fractal image compression algorithm.

The faster compression algorithm is found in the `quadtree` module, while a naive implementation can be used through the `naive` module.
